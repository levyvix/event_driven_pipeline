API_KEY=xxx
RABBIT_HOST=localhost
QUEUE_NAME=weather
POSTGRES_HOST=localhost

# Metabase Configuration (for local development)
# Metabase connects to the same PostgreSQL database as the API
# No additional setup needed; Metabase will auto-detect the database
# Access Metabase at http://localhost:3000 after running docker compose up
# MB_DB_TYPE=postgres
# MB_DB_DBNAME=metabase
# MB_DB_HOST=postgres
# MB_DB_PORT=5432
# MB_DB_USER=weather_user
# MB_DB_PASS=weather_password
